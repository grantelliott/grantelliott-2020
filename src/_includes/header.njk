<header class="container py-12">
    <h1 class="tracking-tight text-lg md:text-xl">
    {% set activePage = page.url %}
    {% if activePage == "/" %}
    <a class="text-indigo-300" href="/">{{ site.title }}</a>
    {% else %}
    <a class="text-indigo-300" href="/">Home</a>
    {% endif %} 
    </h1>
</header>
