<header class="container py-12">
    <h1 class="tracking-tight text-lg md:text-xl mb-8">
    {% set activePage = page.url %}
    {% if activePage == "/" %}
    <a class="opacity-25 md:hover:opacity-100" href="/">{{ site.title }}</a>
    {% else %}
    <a class="opacity-25 md:hover:opacity-100" href="/">Home</a>
    {% endif %} 
    </h1>
    <h2 class="tracking-tight text-2xl md:text-4xl font-bold md:mb-4">
      {{ title }}
    </h2>
  {% if copy %}
    <p class="mt-2 md:text-lg">{{ copy }}</p>
  {% endif %}
</header>
